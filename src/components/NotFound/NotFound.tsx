import React from 'react';
import s from './NotFound.module.scss'
const NotFound = () => {
    return (
        <div className={s.not}>
            404
        </div>
    );
};

export default NotFound ;